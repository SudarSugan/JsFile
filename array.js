let listofgames= ["indoor",'outdoor'];
console.log (listofgames);// it shows the list which is present inside the array

let listofgames1= ["indoor", 'outdoor']
console.log (listofgames1[1]); // it shows the particular array length which is mentation in the len.

listofgames[2]= 'greenmat';
console.log (listofgames)// it help to add the list in the array using the array length 

console.log (listofgames.length);  

function name  (firstName , lastName){
console.log (firstName + lastName );
}
name("Arun", "Kumar");// just declar the function and  call the fuction did the task

function squr (num){
   return num*num;
}
console.log(squr(2)); // it is a fuction (squr) done using parameter (num) did the calculation